import { NgModule } from '@angular/core';

// В проекте используется standalone-конфигурация (bootstrapApplication),
// поэтому NgModule не применяется. Файл оставлен для совместимости с
// инструментами, но не участвует в инициализации приложения.
@NgModule({})
export class AppModule {}
